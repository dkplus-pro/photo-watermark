# 复制为 .env.local 后填写;.env.* 已 gitignore,禁止提交真实密钥(见 docs/site.md「环境变量」)。
# 全部槽位可选:未配置走默认值;配置了但非法(如非 URL)会在启动时抛出带字段名的错误
# (校验与类型化导出见 src/config/env.ts,特性开关见 src/config/features.ts)。

# ---- API ----

# SSR 服务端请求 Go server 的绝对地址(浏览器端走同源相对路径,无需配置)。
# 默认 http://127.0.0.1:8080
# SITE_API_BASE=http://127.0.0.1:8080

# ---- 监控(RUM) ----

# 阿里云 ARMS 用户体验监控:RUM_ENDPOINT 与 RUM_PID 两者均配置才启用上报,
# 任一缺失即关闭(dev 默认关闭;见 docs/site.md「监控」)。
# 注意:客户端配置走构建期内联(modern.config.ts source.define),
# 部署时须在构建(CI)阶段注入这两个变量,仅在运行时注入对浏览器端无效。
# RUM_ENDPOINT=
# RUM_PID=

# ---- 埋点 ----

# 自定义埋点 HTTP 上报端点(sendBeacon);未配置时埋点整体 no-op。
# TRACK_ENDPOINT=
