# Applications

This directory contains runnable applications for the monorepo template.

## Current apps

- `admin` — Modern.js React admin application used to validate workspace build, type-check, lint, and test flows.

Each app should keep application-specific code, tests, and framework configuration inside its own folder so shared packages and root tooling can evolve independently.
